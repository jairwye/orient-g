# 复制为 frontend/.env.local（不入库）
#
# 本地全栈（项目根 uvicorn :8000 + npm run dev）：
API_URL=http://127.0.0.1:8000

# 仅当 backend 跑在其它机器（如实测内网预发 Hermes）时改为：
# API_URL=http://192.168.x.x:8000
#
# 未设置时 frontend 代理默认也是 http://127.0.0.1:8000（见 app/api/[[...path]]/route.ts）
