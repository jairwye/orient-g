# 待更新计划

本文件记录**尚未排期实施**、但已确认方向的产品/技术项。正式规制与任务拆解仍以 `specs/plans/`、`specs/features/` 为准。

---

## 1. Hermes 页面互动书写 Skill 并全站配置

**状态：** 待规划（未实现）

**目标：**

- 用户通过 **AI内网（Agent）页面** 与 Hermes 协作，起草、修订并**提交** Agent Skill（符合 `docs/skills-guidelines.md`）。
- 经评审/权限流程后，技能写入 `backend/data/agent_skills/<skill_id>/` 并登记 `manifest.json`，对**全站内网用户**可配置启用（与现有 `enabled_skills` / 工作空间技能勾选一致）。

**当前缺口：**

| 项 | 现状 |
|----|------|
| MCP 工具 | 无 `orientg_skill_submit` / `orientg_skill_publish` 等；生产 Hermes **不能**写技能 |
| Tier 1 | `skill_view` 在 `hermes_forbidden_tools` 中 |
| 产品流程 | 技能固化依赖 manifest + 目录评审，非页面一键发布 |
| 全站生效 | 需 ACL、版本号、`skill.*` ID 与 Handler 注册表联动 |

**建议后续步骤（概要）：**

1. 规制：`specs/features/` 新增「Skill 草稿 → 评审 → 发布」状态机与安全约束（禁止编造、证据引用、仅内网）。
2. MCP：`orientg_skill_draft_save`、`orientg_skill_submit_review`（或合并为 submit + 后台队列）；写操作带 `hermes_session_key` + 管理员/角色校验。
3. Backend API + DB：草稿表、审计日志、与 `manifest.json` 生成/合并工具链。
4. 前端 AI内网：Skill 编辑/预览/提交 UI；设置页或工作空间「全站默认技能」策略（若与 per-user 勾选并存需定义优先级）。
5. Hermes：Tier 策略调整（是否允许 lite 调 skill 相关 tool）；上游若未来支持 per-call MCP env，可再评估 JWT 注入，**不阻塞**本项（仍以 `hermes_session_key` 为主）。

**关联文档：**

- `docs/skills-guidelines.md`
- `docs/agent-skills-glossary.md`
- `backend/data/agent_skills/manifest.json`
- `docs/hermes.md` §4（多用户 JWT）

---

*最后更新：2026-05-28*
