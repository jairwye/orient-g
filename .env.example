# 数据库（本地开发指向本机 PostgreSQL；生产部署务必修改用户名与密码）
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/mgmt_web

# 上传文件存放目录（本地开发用本地路径，生产在 Docker 中挂载 volume）
UPLOAD_DIR=./uploads

# 可选：内网反向代理或前端地址（用于 CORS 等）
# FRONTEND_ORIGIN=http://localhost:3000
