# 待更新计划

实现某项后，将对应行的 `[ ]` 改为 `[x]` 或在项后注明 **已更新** 及日期。**v1.1** 已完成下列全部项。

1. [x] 财务后台页面默认路径为 `/admin`，该路径可在财务后台页面修改（已更新）
2. [x] 财务后台页面新增：设置用户名及默认密码（已更新）
3. [x] 财务后台页面新增：启用本项目页面需要用户名密码登录的功能；默认存在用户 `admin`、密码 `123456`，防止未设置用户名就开启鉴权后无法登录（已更新）
4. [x] 默认密码在首次登陆时必须要修改（已更新）
5. [x] 增加用户管理页面，允许用户修改目前登录用户的密码并生效（已更新）
6. [x] 会话超时：用户半小时无活动或关闭网站后自动退出登录，再次访问需重新登录（已更新）
7. [x] v1.1：多用户（用户列表、新增/删除/重设默认密码）、管理后台布局调整、登录态 sessionStorage+代理、关闭标签页需重新登录（已更新）
